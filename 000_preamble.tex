
% ------------------------------
% Packages
% ------------------------------
\usepackage[ngerman]{babel}
\usepackage[T1]{fontenc}

% Schriftart
\usepackage{fontspec}

% Times New Roman
\setromanfont{Times New Roman}
% Arial
\setsansfont{Arial}
% Courier New
\setmonofont{Courier New}


% SVG
\usepackage{svg}

% Zeilenabstand
\usepackage{setspace}
\setstretch{1.5}

% Silbentrennung
\usepackage[babel]{microtype}
\emergencystretch=1em

% Keine Einrückung am Paragraphbeginn, stattdessen Absatz
% Dieses Paket sorgt für eine Warnung, allerdings lässt erzeugt
% die von der KOMA-Klasse mitgelieferte parskip-Option auch 
% Abstand zwischen Überschrift und der zugehörigen Section.
% Das ist nicht erwünscht. Deshalb trotzdem Nutzung dieses Pakets.
\usepackage{parskip}

% Abkürzungsverzeichnis
\usepackage[acronyms, toc, nonumberlist, nopostdot, nogroupskip]{glossaries}

% Harvard Zitierstil
\usepackage[date=year,citestyle=mycitationstyle,bibstyle=mycitationstyle,maxcitenames=2,maxbibnames=99,giveninits=true,doi=false,isbn=false,url=true,eprint=false,uniquename=false]{biblatex}
\addbibresource{references.bib}
\renewcommand{\newunitpunct}{, }
\renewcommand{\finalnamedelim}{\multinamedelim}

% Seitenformat
\usepackage[left=30mm,top=25mm,right=25mm,bottom=15mm]{geometry}

% Schriftgröße Sections
\addtokomafont{section}{\fontsize{14}{16.8}\selectfont}
\addtokomafont{subsection}{\fontsize{12}{15.6}\selectfont}
{\fontsize{12}{15.6}\selectfont}

% Header und Footer Custom
\usepackage{fancyhdr}
\fancyhf{} % Void all fields in header and footer
\fancyhead[R]{\thepage} % Seitennummer oben rechts

% Farbe für rote Anmerkungen
\usepackage{color}

% Quellcode
\usepackage{listings}
\lstset{
    basicstyle=\ttfamily\small\linespread{1}\selectfont,
    numbers=left,
    frame=single,
    breaklines=true,
    breakatwhitespace=false,
    numberstyle=\footnotesize
}

%% Java etwas schöner
\definecolor{javared}{rgb}{0.6,0,0} % for strings
\definecolor{javagreen}{rgb}{0.25,0.5,0.35} % comments
\definecolor{javapurple}{rgb}{0.5,0,0.35} % keywords
\definecolor{javadocblue}{rgb}{0.25,0.35,0.75} % javadoc

\lstdefinestyle{Java}{
    basicstyle=\ttfamily,
    keywordstyle=\color{javapurple}\bfseries,
    stringstyle=\color{javared},
    commentstyle=\color{javagreen},
    morecomment=[s][\color{javadocblue}]{/**}{*/},
}

%% JavaScript
\definecolor{lightgray}{rgb}{.9,.9,.9}
\definecolor{darkgray}{rgb}{.4,.4,.4}
\definecolor{purple}{rgb}{0.65, 0.12, 0.82}

\lstdefinelanguage{JavaScript}{
    keywords={typeof, new, true, false, catch, function, return, null, catch, switch, var, if, in, while, do, else, case, break},
    keywordstyle=\color{blue},
    ndkeywords={class, export, boolean, throw, implements, import, this},
    ndkeywordstyle=\color{darkgray}\bfseries,
    identifierstyle=\color{black},
    sensitive=false,
    comment=[l]{//},
    morecomment=[s]{/*}{*/},
    commentstyle=\color{purple}\ttfamily,
    stringstyle=\color{red}\ttfamily,
    morestring=[b]',
    morestring=[b]"
}

\lstdefinestyle{JavaScript}{
    numberstyle=\small,
}

% Grafiken
\usepackage{graphicx}

% Klickbare Links
\usepackage[hidelinks]{hyperref}

% Nummerierung kapitelweise (Abbildungen, ...)
\usepackage{chngcntr}
\counterwithin{figure}{section}

% Url Style im Literaturverzeichnis
\usepackage{url}
\urlstyle{same}

% Bessere Positionierung von Bildern
\usepackage{float}

% Entfernen von Warnungen (ich weiß nicht, warum das hilft)
\usepackage{scrhack}
\usepackage{csquotes}

% Zeilenabstand in itemize
\usepackage[]{enumitem}
\setlist[itemize]{topsep=-10pt, nosep} 

% Bessere Formeln
\usepackage{amsmath}

% ------------------------------
% New Commands
% ------------------------------

% Unten links auf Titelseite
\newcommand{\keyvalue}[2]{
    #1: & #2 \\
}

% rot
\newcommand{\red}[1]{\textcolor{red}{ #1}}

% Anführungszeichen
\newcommand{\q}[1]{\glqq #1\grqq{}}

% Listing
\newcommand{\showlisting}[3]{
    \lstinputlisting[caption={#1}, label=#2, float, captionpos=b]{#3}
}

\newcommand{\showlistinglang}[4]{
    \lstinputlisting[language=#4, caption={#1}, label=#2, float=tp, captionpos=b, xleftmargin=2em, framexleftmargin=2em]{#3}
}
\newcommand{\showlistinglangappendix}[4]{
    \lstinputlisting[nolol, language=#4, caption={#1}, label=#2, captionpos=b, xleftmargin=2em, framexleftmargin=2em]{#3}
}

% \newcommand{\codefont}[1]{
%     \lstinline|#1|
% }

% ------------------------------
% Miscellaneous
% ------------------------------
\makenoidxglossaries

\newcounter{saveromanpages}

% Graphikpfad
\graphicspath{}

% Absätze, die nach einem Seitenumbruch nur noch ein paar Wörter auf der nächsten Seite haben, und sowas, verhindern
\clubpenalty=10000
\widowpenalty=10000
\displaywidowpenalty=10000